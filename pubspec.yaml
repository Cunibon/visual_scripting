name: visual_scripting
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  localstorage: ^4.0.1+4
  provider: ^6.1.1
  vs_node_view:
    path: F:/FlutterProjekt/vs_node_view

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
